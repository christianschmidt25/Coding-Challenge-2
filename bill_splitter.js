// Task 1: Calculate the Tip

const x = "bill";
let tip = (50 <= x <= 300) ? *0.15 : *0.2;

// "tip" will be multiplied by 0.15 IF "x" (bill) is in between 50 and 300, otherwise it will be multiplied by 0.20.


// Task 2: Output Details

